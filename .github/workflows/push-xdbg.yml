name: Push XDBG Image

on:
  push:
    branches:
      - main
    paths:
      - 'apps/xmtp_debug/**'
      - 'crates/xmtp_api/**'
      - 'crates/xmtp_mls/**'
      - 'crates/xmtp_db/**'
      - 'crates/xmtp_id/**'
      - 'crates/xmtp_cryptography/**'
      - 'crates/xmtp_proto/**'
      - 'crates/xmtp_api_grpc/**'
      - 'crates/xmtp_api_d14n/**'
      - 'crates/xmtp_configuration/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: xmtp/xdbg

jobs:
  shellcheck:
    name: Lint shell scripts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: ludeeus/action-shellcheck@2.0.0
        with:
          scandir: './apps/xmtp_debug/docker'
          severity: warning

  build:
    name: Build and push xdbg image
    runs-on: ubuntu-latest
    needs: shellcheck
    outputs:
      digest: ${{ steps.push.outputs.digest }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Capture commit metadata
        id: vars
        run: echo "short_sha=$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=${{ steps.vars.outputs.short_sha }}
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      - name: Build and push image
        id: push
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./apps/xmtp_debug/docker/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy xdbg to ${{ matrix.environment }}
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'
    strategy:
      matrix:
        environment: [testnet-staging, testnet]
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Deploy to ${{ matrix.environment }}
        uses: xmtp-labs/terraform-deployer@v1
        timeout-minutes: 45
        with:
          timeout: 45m
          terraform-token: ${{ secrets.TERRAFORM_TOKEN }}
          terraform-org: xmtp
          terraform-workspace: ${{ matrix.environment }}
          variable-name: "client_monitor_image"
          variable-value: "ghcr.io/${{ env.IMAGE_NAME }}@${{ needs.build.outputs.digest }}"
          variable-value-required-prefix: "ghcr.io/xmtp/"
