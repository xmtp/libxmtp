[profile.default]
default-filter = "all()"
retries = 0

[profile.ci]
default-filter = "all()"
retries = 0
status-level = "skip"
failure-output = "immediate-final"
fail-fast = false


[[profile.ci.overrides]]
platform = 'wasm32-unknown-unkown'
slow-timeout = "1m"
