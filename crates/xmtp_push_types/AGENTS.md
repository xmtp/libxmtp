# xmtp_push_types

Shared push payload and JSON encoding. No database or network dependencies.

```bash
dev/nix-shell 'just check crate xmtp_push_types'
dev/nix-shell 'just test crate xmtp_push_types'
```
