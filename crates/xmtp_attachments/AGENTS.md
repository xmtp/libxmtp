# xmtp_attachments

Streaming attachment encryption, envelope parsing, and path derivation.

Run `dev/nix-shell 'source dev/sccache-env && just test crate xmtp_attachments'`.
Use `CARGO_BUILD_JOBS=5` when other lanes build.
