# xmtp_attachments_server

This crate validates attachment storage settings and signs S3-compatible PUT
requests. Run `dev/nix-shell 'just test crate xmtp_attachments_server'` from the
repository root. Do not log credentials or presigned URLs.
