{
  "name": "browser",
  "devDependencies": {
    "@vitest/browser": "^3.2.1",
    "@xmtp/wasm-bindings": "workspace:^",
    "playwright": "^1.52.0",
    "vitest": "^3.2.1"
  },
  "scripts": {
    "rollup": "rollup -c",
    "test": "yarn build:test && vitest"
  }
}
