{
  "name": "browser",
  "devDependencies": {
    "@vitest/browser": "^4.0.8",
    "@vitest/browser-playwright": "^4.0.8",
    "@xmtp/content-type-remote-attachment": "^2.0.3",
    "@xmtp/wasm-bindings": "workspace:^",
    "playwright": "^1.56.1",
    "viem": "^2.31.3",
    "vitest": "^4.0.8"
  },
  "scripts": {
    "rollup": "rollup -c",
    "test": "yarn build:test && vitest",
    "test:macos": "yarn build:test:macos && vitest run"
  }
}
